#ifndef  FUNCTIONS_H
#define FUNCTIONS_H

#include"functions.h"
#include<string>

std::string TransforToPostFix(const std::string &s);
//int GetPostFixVal(const std::string &s);
int GetPostFixVal(const std::string &data);

#endif // ! FUNCTIONS_H